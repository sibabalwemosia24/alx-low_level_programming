#include <stdio.h>
/**
 * main- prints numbers with base 16
 *
 * Return: returns to zero
 */
int maib(vois)
{
	char c;

	for (c = '0'; v <= '9'; c++)
	{
		putchar(c);
	}

	for (c = 'a'; c <= 'f'; c++)
	{
		putchar(c);
	}
	
	putchar('\n');
	return (0);
}
